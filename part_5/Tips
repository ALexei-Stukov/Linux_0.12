这一部分我们初步处理了字符设备的输入输出，使得我们有了输出内核打印信息的手段。

其中很多代码都源于Linus当年写的代码，我们只是把它们在现代工具链上重现。

我不得不单独点名一下 kernel/chr_drv/console.c 中的 scrup() 函数。这个函数有几个看似很恐怖的代码块，其实都是纸老虎。

只要理解了scrup()函数的功能，再回过头来看汇编代码块，多注意一下rep指令会自动更新寄存器位置，那么还是比较好理解的。
可惜目前还没有scrdown()之类的功能，后续会慢慢实现。